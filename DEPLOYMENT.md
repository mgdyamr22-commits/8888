# Almakhzoun Inventory Pro — دليل النشر والتشغيل (PHP 8.0+ & MySQL)
**النظام:** نظام المخزون الاحترافي للسيارات (Almakhzoun Inventory Pro)  
**البيئة المستهدفة:** استضافة مشتركة / cPanel / Apache / LiteSpeed / Nginx + PHP 8.0+ + MySQL  

---

## 1. متطلبات الاستضافة (Hosting Requirements)
- **PHP:** إصدار PHP 8.0 أو 8.1 أو 8.2 أو 8.3.
- **امتدادات PHP المطلوبة:** `pdo`, `pdo_mysql`, `json`, `mbstring`, `openssl`.
- **قاعدة البيانات:** MySQL 5.7+ أو MySQL 8.0+ أو MariaDB 10.3+.
- **خادم الويب:** Apache مع تفعيل `mod_rewrite` أو LiteSpeed أو Nginx.

---

## 2. خطوات الرفع على الاستضافة (cPanel / Hostinger / VPS)

### الخطوة 1: بناء ملفات الواجهة (Build)
يتم تشغيل أمر البناء لإنتاج الملفات الجاهزة:
```bash
npm run build
```
ينتج عن ذلك مجلد `dist/` يحتوي على ملفات الواجهة المترجمة (`index.html` ومجلد `assets/`).

### الخطوة 2: رفع الملفات إلى مجلد الموقع العام (`public_html`)
انقل محتويات المجلدات التالية إلى مسار موقعك الرئيسي (مثل `public_html`):
1. **محتويات مجلد `dist/`**: (ملف `index.html` ومجلد `assets/`).
2. **مجلد `api/`**: بكامل ملفات الـ PHP والـ endpoints.
3. **مجلد `config/`**: لإعدادات النظام والاتصال.
4. **مجلد `database/`**: لملفات الهجرة والمخطط التلقائي.
5. **مجلد `storage/`**: لتخزين المرفقات والنسخ الاحتياطية.
6. **ملف `.htaccess`**: المسؤول عن توجيه المسارات والحماية.

> **تنبيه:** تأكد أن صلاحيات مجلد `storage/` ومجلد `config/` هي `755` أو `775` (قابلة للكتابة).

---

## 3. التثبيت التلقائي عبر المتصفح (Web Installer)

1. أنشئ قاعدة بيانات MySQL واسم مستخدم جديد من لوحة تحكم الاستضافة (cPanel -> MySQL Databases).
2. افتح رابط موقعك في المتصفح: `https://your-domain.com`.
3. سيكتشف النظام عدم وجود قاعدة بيانات تلقائياً ويفتح لك **معالج التثبيت الذكي**:
   - **الخطوة 1:** التحقق التلقائي من توافق PHP وامتدادات السيرفر وصلاحيات المجلدات.
   - **الخطوة 2:** إدخال بيانات MySQL (Host, Database, User, Password) مع اختبار اتصال فوري.
   - **الخطوة 3:** إنشاء جميع جداول النظام ومخطط العلاقات آلياً بدون الحاجة لاستيراد SQL يدوياً.
   - **الخطوة 4:** إنشاء حساب المدير العام (اسم المستخدم، كلمة المرور).
   - **الخطوة 5:** حفظ بيانات المؤسسة (الاسم، السجل التجاري، الرقم الضريبي، الشعار والختم).
4. بعد اكتمال التثبيت، يتم تحويلك تلقائياً لصفحة تسجيل الدخول ويبدأ العمل فوراً!

---

## 4. حل المشاكل الشائعة (Troubleshooting)

### المشكلة: شاشة سوداء عند فتح الموقع
- **السبب:** تم رفع ملفات الكود المصدري الأصلية (`index.tsx`) بدلاً من رفع ملفات الواجهة المترجمة الناتجة في مجلد `dist/`.
- **الحل:** تأكد من رفع ملف `dist/index.html` ومجلد `dist/assets/` إلى المسار الرئيسي للاستضافة `public_html/`.

### المشكلة: خطأ 404 عند استدعاء `/api/*`
- **السبب:** ملف `.htaccess` غير موجود أو `mod_rewrite` غير مفعل على السيرفر.
- **الحل:** تأكد من وجود ملف `.htaccess` في جذر الموقع ومن تفعيل خيار إظهار الملفات المخفية في مدير ملفات cPanel.

### المشكلة: فحص صحة الخادم مباشرة
يمكنك دائماً التحقق من عمل الخادم عبر فتح:
`https://your-domain.com/api/health.php`
ستظهر لك استجابة JSON تفيد بحالة PHP و MySQL.
- **Manual Web Export:** Available from `لوحة التحكم > الصيانة والنسخ الاحتياطي` in `.json` and `.sql` compressed archive format.
- **Disaster Recovery:** Full point-in-time database restoration via the Backup Manager with atomic rollback.
