# تقرير إصلاح وتطوير نظام تسجيل الدخول إلى Google Drive و Google OAuth 2.0

## 1. سبب المشكلة السابق
كان النظام يستند سابقاً إلى آليات غير مخصصة لبيئة سطح المكتب (Desktop Apps) مثل النوافذ المنبثقة التفاعلية للويب (web popups) أو إجبار المستخدم على نسخ وطلب الـ `Access Token` يدوياً وإدخاله في نافذة إعدادات البرنامج.
- تسببت القيود الناتجة عن سياسات Google المتعلقة بالأمان في بيئات سطح المكتب (`disallowed_useragent` أو تعذر الـ `redirect_uri` التلقائي) في تعطل الاتصال التلقائي.
- غياب سير عمل الـ **PKCE (Proof Key for Code Exchange)** وخادم الـ **Loopback المحلي (`http://127.0.0.1:port/oauth2callback`)**.

---

## 2. الملفات التي تم تعديلها وتطويرها

1. **`main.js` (المحرك الرئيسي لـ Electron Backend):**
   - تم إنشاء خادم استماع محلي مؤقت dynamically allocated Loopback HTTP Server على عنوان `127.0.0.1` لاستقبال الـ `Authorization Code` فور موافقة المستخدم في المتصفح الخارجي الرسمي.
   - تم دمج معيار **PKCE** (توليد `code_verifier` و `code_challenge` من نوع `S256`) لرفع مستوى أمان طلبات التفويض.
   - تم تحديث بيانات اعتماد التطبيق (`clientId` و `clientSecret`) الرسمية الخاصة بالعميل.
   - تم بناء نظام حفظ وإدارة التوكن تلقائياً داخل مسار البيانات المشفرة `userData/oauth_tokens.json`.
   - إضافة دالة تجديد تلقائي للـ Token (`refreshAccessToken`) عند قرب انتهاء صلاحية الـ Access Token (قبل 5 دقائق من انتهاء الصلاحية) باستخدام الـ `Refresh Token` المخزن آمنياً.
   - تسجيل جميع استثناءات وأخطاء عملية التفويض والربط في ملف سجلات مخصص: `logs/oauth.log` مع تسجيل كامل لـ Stack Trace ومكواد الخطأ وتجربة الاستجابة من Google.

2. **`preload.js` (الجسر بين Electron والـ Renderer):**
   - إضافة قنوات IPC الآمنة:
     - `googleOauthStart`: لبدء عملية التفويض وفتح متصفح النظام الرسمي.
     - `googleOauthGetToken`: لجلب Access Token صالح تلقائياً (أو تجديده).
     - `googleOauthLogout`: لتسجيل الخروج وتصفير التوكن المخزن.
     - `googleOauthStatus`: للاستعلام عن حالة الربط الحالية وحساب الإيميل المرتبط.

3. **`src/utils/googleDriveService.ts`:**
   - ربط وظائف Google Drive API بالجسور البرمجية التلقائية الجديدة لـ Electron.
   - توفير دالة `getActiveGoogleAccessToken` التي تجلب الـ Access Token الصالح دائماً وتتعامل مع التجديد التلقائي خلف الكواليس دون أي تدخل بشري.

4. **`components/BackupManager.tsx`:**
   - **حذف نافذة إدخال Google Access Token اليدوية بالكامل** وإزالة كافة الأزرار والرموز الحوارية المتعلقة بالإدخال اليدوي.
   - تحديث واجهة المستخدم لتعمل بالكامل بنقرة زر واحدة ("ربط حساب Google Drive") والتي تفتح المتصفح الرسمي، وعند نجاح الربط يتم التفعيل والتكتم التلقائي على الـ Token وإتاحته لكافة عمليات المزامنة اليومية واللحظية.

---

## 3. كيف أصبح الربط يعمل تلقائياً؟

1. **الضغط على الزر:** يضغط المستخدم على "ربط حساب Google Drive".
2. **الفتح في المتصفح الرسمي:** يقوم Electron بإنشاء سيرفر محلي مؤقت وإعطاء أمر لمتصفح النظام الافتراضي (Chrome / Edge / Firefox) بفتح صفحة تسجيل دخول Google الرسمية المعززة بـ PKCE.
3. **الموافقة على الصلاحيات:** يسجل المستخدم دخوله ويمنح الصلاحيات الصريحة للتطبيق (أقل نطاق ممكن: `https://www.googleapis.com/auth/drive.file`).
4. **الاستقبال والتوليد التلقائي:** يستقبل السيرفر المحلي الـ `Authorization Code` ثم يستبدله فوراً بـ `Access Token` و `Refresh Token` ومعلومات الحساب.
5. **الحفظ المشفر والتجديد التلقائي:** يتم حفظ الـ `Refresh Token` آمنياً في `userData`. وعند قيام التطبيق بأي عملية رفع أو مزامنة، يتم فحص صلاحية التوكن وتجديده تلقائياً خلف الكواليس عند الحاجة دون مطالبات أو إزعاج للمستخدم.

---

## 4. التأكيد والتعهد التقني
- **لن يحتاج المستخدم إلى إدخال Access Token يدوياً نهائياً.**
- النظام أصبح مدركاً تماماً لسيناريوهات انقطاع الإنترنت، انتهاء صلاحية التوكن، وتغيير الحساب أو تسجيل الخروج بشكل دائم ومنظم.
